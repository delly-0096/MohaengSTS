<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.mohaeng.mypage.notifications.mapper.INotiAlarmMapper">


<select id="selectAlarmCount"
        parameterType="kr.or.ddit.mohaeng.vo.PaginationInfoVO"
        resultType="int">
		  SELECT COUNT(*)
		  FROM ALARM
		  WHERE MEM_NO = #{memNo}
</select>

  <select id="selectUnreadAlarmCount" resultType="int" parameterType="int">
    SELECT COUNT(*)
    FROM ALARM
    WHERE MEM_NO = #{value}
      AND NVL(READ_YN,'N') = 'N'
  </select>

  <select id="selectAlarmList"
          parameterType="kr.or.ddit.mohaeng.vo.PaginationInfoVO">
      <![CDATA[   
    SELECT *
    FROM (
      SELECT t.*, ROWNUM rn
      FROM (
        SELECT
          ALARM_NO,
          MEM_NO,
          SENDER,
          ALARM_TYPE,
          ALARM_CONT,
          MOVE_URL,
          READ_YN,
          READ_DT,
          REG_DT
        FROM ALARM
        WHERE MEM_NO = #{memNo}
        ORDER BY REG_DT DESC
      ) t
      WHERE ROWNUM >= #{endRow}
    )
    WHERE rn <= #{startRow}
      ]]>
  </select>

</mapper>
